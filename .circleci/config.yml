version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:2026.03
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: "Docker compose"
          command: cd services && docker compose up -d

workflows:
  build-and-test:
    jobs:
      - build